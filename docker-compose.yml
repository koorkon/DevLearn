version: '3.8'

services:
  backend:
    image: ${{ secrets.koorkon }}/devlearn-backend:latest
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
    restart: always

  frontend:
    image: ${{ secrets.koorkon }}/devlearn-frontend:latest
    ports:
      - "80:5173" 
    depends_on:
      - backend
    restart: always